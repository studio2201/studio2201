name: boneyard
on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  boneyard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: ./
        with:
          tools: 'boneyard'
          fail-on: 'block'
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: boneyard-report
          path: boneyard_report.md
          if-no-files-found: ignore
