# Section 11 · What gets inherited, not re-decided



From the parent studio2201 doctrine:

- `RULES.md §1.1` first principles — every service starts from one question
- `RULES.md §1.2` act every turn — pre-release scaffolds are honest
- `RULES.md §1.3` honesty — placeholder axes are labeled with their `source`
- `RULES.md §1.4` security — defaults fail closed
- `RULES.md §1.5` keep things clean — files get shorter over time, not longer

These five rules are non-negotiable for the five services. Anything more is product-specific and lives in that product's `docs/architecture.md`.
